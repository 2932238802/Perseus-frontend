# 现状问题

- ctrl + 左键的时候 鼠标总是显示 "点击形式"
  - 期待: 应该和vscode的行为保持一致
- 设置ui 左侧的"Editor" "Terminal"等 会被遮挡 显示不全 窗口偶尔从左上角弹出
- 设置ui 左侧的Format的栏目，默认显示的应该是FORMAT_STYLE(LLVM_formatStyle) 用户可以进行修改
- Install Tools 的栏目点击Install 应该进行安装 但是安装的时候 往往就要求 输入sudo的密码 我希望需要输入密码的时候 有一个弹窗 用户输入完密码之后 这个密码可以同步输入到终端去 进行正常安装 安装完毕之后 也能发送一个信号 更新 Install Tools 
- Terminal Setting是空的，可以进行哪些配置呢？

# 解决:

- 针对这几个问题，请你在根目录生成 SOLUTION.md 针对这几个问题分别进行解答和思路设计

# 设计模式推荐：

- 适度使用现代设计模式，不能为了模式而模式
- PImpl：用于设置控制器 安装服务等私有实现较复杂的类 减少头文件依赖
- Strategy：用于不同终端类型、不同安装执行后端
- Factory：用于根据工具类型创建安装任务或终端配置
- Command：用于封装单个工具安装动作，支持状态、输出、取消和重试
- Observer：使用 Qt signal/slot 实现，不额外引入观察者基类
- Plugin：仅为未来扩展工具安装器或终端后端预留接口，不在本阶段引入动态插件系统
- State Machine：用于安装流程状态管理，避免多个 bool 变量互相冲突